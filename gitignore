# ============================================================================
#  .gitignore — NÄFELS
# ============================================================================

# ── NUNCA subir la llave de los datos ───────────────────────────────────────
# Si esto se sube, el cifrado no sirve para nada: queda la llave al lado
# de los archivos cifrados.
LLAVE.txt

# ── Copias de respaldo que dejan los scripts ────────────────────────────────
*.antes

# ── Archivos temporales que NO se suben ─────────────────────────────────────
__pycache__/
*.pyc
diagnostico.txt
_gen_nafels.b64
*.log
