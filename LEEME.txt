DOS COSAS
=========

1) EL ARCHIVO
   dashboard.html a la carpeta y publicar.

   Arregla el "ReferenceError: s is not defined" que dejaba el resumen del
   equipo en blanco. Mi reemplazo automatico habia generado VB_EFF.saque(s),
   tomando la "s" de sPunto como si fuera el nombre de un objeto. Ahora se
   arma el objeto con los totales sueltos y se lo pasa completo.


2) HAY QUE CORRER HACER_TODO
   Esto NO es un archivo, es un paso que falta.

   datos_baterias.js todavia tiene la escala VIEJA:

     saque      -25   (deberia dar 39)
     recepcion   11   (deberia dar 55)

   El motor gen_baterias.py ya esta publicado con la escala 0-100, pero el
   archivo de datos se genera al correr HACER_TODO y eso no se hizo desde
   entonces. Por eso las BATERIAS muestran numeros negativos y se pintan
   rojas, aunque las tarjetas de arriba ya esten bien.

   Correr HACER_TODO y publicar. Ahi las baterias pasan a la misma escala
   que el resto y los colores dejan de dar rojo.


VERIFICADO EN ESTA TANDA
------------------------
  · sintaxis de las 8 pantallas + objetivos_config.js: OK
  · las 30 llamadas a VB_EFF, una por una: todas con su objeto declarado
  · dashboard: 17 llamadas, todas correctas
  · la funcion unica devuelve lo mismo con los 5 juegos de nombres
